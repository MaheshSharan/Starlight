# API Configuration
VITE_API_URL=http://localhost:3001

# App Configuration
VITE_APP_NAME=Starlight
VITE_APP_VERSION=1.0.0

# Development Configuration
VITE_DEV_MODE=true
VITE_ENABLE_DEVTOOLS=true

# Note: TMDB API key and configuration should be on the backend only
# The frontend only needs to know about our own API endpoint